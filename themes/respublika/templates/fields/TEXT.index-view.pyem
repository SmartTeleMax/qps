<%
if linkThrough:
    %><a href="<%= template.edit.prefix + item.path() %>"><%
%><%= quoteHTML(value[:brick.lengthInIndex]) + '...' * (len(value) > brick.lengthInIndex) %><%
if linkThrough:
    %></a><%
%>
