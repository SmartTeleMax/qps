<%
if value:
    if brick.linkThrough:
        %><a href="<%= quoteHTML(edPrefix+item.path()) %>"><%
    %><img src="<%= quoteHTML(edPrefix+value.path) %>"><%
    if brick.linkThrough:
        %></a><%
%>
