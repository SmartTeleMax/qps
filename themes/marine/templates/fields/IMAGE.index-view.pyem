<%
if value:
    if brick.linkThrough:
        %><a href="<%= quoteHTML(template.edit.prefix+item.path()) %>"><%
    %><img src="<%= quoteHTML(template.edit.prefix+value.path) %>"><%
    if brick.linkThrough:
        %></a><%
%>
