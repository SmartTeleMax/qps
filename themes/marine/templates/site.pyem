<%= brick.template('inc_top') %>

<h1>Editor interface of <q><%= brick.site.title %></q></h1>

<table class="centermenu">
<%
for stream in brick.allowedStreams:
    if not getattr(stream, 'showInMenu', 0):
        %><tr><td><a href="<%= brick.edPrefix+stream.path() %>"><%= stream.title %></a></td></tr>
<%
%></table>

<%= brick.template('inc_bottom') %>
