<%
if brick.linkThrough:
    %><a href="<%= edPrefix + item.path() %>"><span class=t12 id=db1><%= quoteHTML(value[:brick.lengthInIndex]) + '...' * (len(value) > brick.lengthInIndex) %></span><%
if brick.linkThrough:
    %></a>
