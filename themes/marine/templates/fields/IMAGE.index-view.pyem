if value:
    if brick.linkThrough:
        %><a href="<%= '%s%s' % (edPrefix, item.path())  %>"><%
    %><img src="<%= edPrefix %><%= quoteHTML(value.path) %>"><%
    if brick.linkThrough:
        %></a><%
%>
